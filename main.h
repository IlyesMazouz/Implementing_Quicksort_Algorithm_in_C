#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

// all the functions needed to perform the Quicksort Algorithm

int partition(int arr[], int low, int high);
void quicksort(int arr[], int low, int high);

#endif